package com.sample.service;

import com.sample.config.AppConfig;

public class AppService extends BaseService<AppConfig> {
    public AppService(AbstractServiceBundle bundle) {
        super(bundle);
    }
}
